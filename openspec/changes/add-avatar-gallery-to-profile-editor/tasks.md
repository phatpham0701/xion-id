# Tasks

- [x] Inspect profile editor, onboarding avatar picker, avatar helpers, and available dialog/tabs UI primitives.
- [x] Add a profile-editor avatar gallery dialog that reuses `AvatarPicker`.
- [x] Save selected built-in avatar paths to `profiles.avatar_url` without changing upload behavior.
- [x] Add OpenSpec proposal and task documentation.
- [x] Run lint, tests, and production build (blocked by missing/forbidden npm packages in the environment).
