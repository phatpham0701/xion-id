# Tasks
- [x] Inspect current Admin Badges behavior.
- [x] Confirm Sport Lifestyle catalog source (`SPORT_BADGES`).
- [x] Add admin catalog manager UI.
- [x] Add local demo catalog overrides/custom badge support.
- [x] Allow issuing selected catalog badges to profiles.
- [x] Preserve existing issued badge table and remove flow.
- [x] Avoid `wallet_badges(profile_id, kind)` collisions by merging multiple catalog badges into metadata on the mapped safe enum row.
- [ ] Run CI/build. (Attempted `npm install`, `npm run build`, and `npm run lint`; dependency installation is blocked by 403 registry access in this environment.)
