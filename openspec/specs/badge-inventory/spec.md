# Badge Inventory Spec

## Requirements
- `wallet_badges` is the source of truth for admin-issued badges.
- Admin-issued badges must appear in user badge inventory.
- Whale badge kind must render correctly.
- Admin can view all badges owned by a profile without raw `profile_id`.
