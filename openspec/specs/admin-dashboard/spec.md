# Admin Dashboard Spec

## Requirements
- Admin can access `/admin` only if authorized.
- Admin can view users/profiles.
- Admin can issue/remove badges safely.
- Admin can view current badges owned by each profile.
- Destructive actions require confirmation.
- Admin health/demo pages are internal only.
